import React from 'react';

const Products = ({products, onAddToCartClicked}) => {
    
    return (
        <button onClick={() => addProductToCart({name: "Just a product name"})}>{text}</button>
    );
}